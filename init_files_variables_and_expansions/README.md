# Shell - Init files, variables and expansions

This project focuses on understanding shell initialization files, managing variables, and using expansions in Bash.

## Scripts description:

- **0-alias**: A script that creates an alias named 'ls' with the value 'rm -f *'.
